package com.tinybit.service.validation;

import com.tinybit.model.Detail;

public class ValidationServiceImpl implements ValidationService {

    @Override
    public boolean isValid(Detail detail) {
        // TODO : Add list length validation
        return true;
    }
}
